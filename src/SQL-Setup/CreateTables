CREATE TABLE Users(
UserName TEXT PRIMARY KEY NOT NULL,
FirstName TEXT,
LastName TEXT,
Password TEXT NOT NULL
) WITHOUT ROWID;

CREATE TABLE Orders(
OrderNumber INTEGER PRIMARY KEY NOT NULL,
Burritos INTEGER,
Fries INTEGER,
Sodas INTEGER,
Meals INTEGER,
Collected BOOLEAN,
Price REAL
) WITHOUT ROWID;

CREATE TABLE UserOrders(
UserName TEXT,
OrderNumber INTEGER,
PRIMARY KEY (UserName, OrderNumber),
FOREIGN KEY (UserName) REFERENCES Users(UserName),
FOREIGN KEY (OrderNumber) REFERENCES Orders(OrderNumber)
) WITHOUT ROWID;


INSERT INTO Users (UserName, FirstName, LastName, Password)
VALUES ("Mainies", "Sam", "Mainwood", "1234");

INSERT INTO Orders(OrderNumber, Burritos, Fries, Sodas, Meals, Collected, Price)
VALUES (1, 1, 1, 1, 0, false, 30.50);

INSERT INTO UserOrders(UserName, OrderNumber)
VALUES("Mainies", 1);

SELECT * FROM Users;
SELECT * FROM Orders;
SELECT * FROM UserOrders;